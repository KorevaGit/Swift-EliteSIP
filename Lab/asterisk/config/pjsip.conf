; Намеренно пустой.
;
; Asterisk 16 автозагружает и chan_pjsip, и chan_sip, и оба хотят порт 5060.
; Проще всего не выгружать модуль, а не дать pjsip ни одного transport —
; тогда он загружается, но ничего не слушает, и 5060 достаётся chan_sip.
;
; EliteSIP работает через chan_sip, потому что так настроен боевой Asterisk.
