# Административный режим

M7c. Настройки разделены на две части: что меняет менеджер сам и что закрыто
паролем администратора. До этого этапа настройки были одни на всех — любой,
кто открыл окно, мог сменить домен АТС, стереть макросы или выключить проверку
сертификата TLS.

## Граница — это два разных окна

Согласовано: открытое и закрытое живут в разных окнах, а не в одной форме с
погашенными строками.

Секционная граница была бы дешевле в правке — вкладки остались бы как есть, а
закрытые секции просто скрывались. Отвергнута по одной причине: она размазана.
Каждая следующая настройка требовала бы отдельного решения, куда её отнести, и
это решение однажды просто забыли бы принять — новая строка молча оказалась бы
на той стороне, где её никто не ждал.

**Кнопки разделов, а не системный `TabView`.** Системная панель вкладок при
нехватке ширины схлопывает лишние в шеврон «»», и на окне 700 pt с шестью
разделами схлопывались почти все. Раздел, спрятанный за раскрывашкой, на чужом
рабочем месте просто не находят: администратор ищет «Доступ», не видит его и
решает, что пароль задать негде. Поэтому бар свой, и все шесть кнопок видны
всегда. Выбранная отмечена галочкой, а не только стилем кнопки: акцентного стиля
на Catalina нет вовсе, и без галочки выбранный раздел там ничем не отличался бы.

**Почему именно окно, а не вкладки в том же окне.** У закрытой части другой
порядок работы: правки копятся и применяются кнопкой (см. ниже), а менеджерские
настройки применяются сразу. В одном окне это выглядело бы как неисправность —
половина настроек «залипает», половина нет. Разные окна делают разницу
очевидной, не объясняя её текстом.

Следствие: **закрытых вкладок в окне настроек не существует вовсе**, они не
показываются серыми. Погашенная вкладка читается как «сломалось», а не как «не
ваше», и порождает звонок в поддержку ровно там, где его хотели избежать.

#**«Управление» — последней секцией внутри прокрутки, а не закреплённым
подвалом.** Закреплённый подвал стоял бы перед глазами менеджера всё время, а
это дверь не для него. Кнопка обычная, не акцентная: акцент на этой странице
принадлежит самопроверке голоса и «Исправить сеть» — тому, чем менеджер
действительно пользуется. Дорога к закрытым настройкам должна быть доступной, а
не заметной.

## Что видит менеджер

`ManagerSettingsView` — единственная форма менеджерских настроек, и другой
нет нигде:

**Вкладки «как видит менеджер» в «Управлении» не существует** (решение этапа 5,
[ui-plan.md](ui-plan.md)). Она была здесь записана и так и не появилась, а
вместо неё менеджерские настройки оказались размазаны дублями по закрытым
разделам: микрофон, наушники, AGC и отпускание устройства стояли в обоих окнах,
рингтон целиком жил внутри закрытых «Входящих». Дубли разошлись по текстам за
один этап, и администратор с менеджером видели разные формулировки одной
настройки.

Следствие: **пока «Управление» открыто, менеджерское окно не открывается** —
пункт меню и кнопка панели гаснут, ⌘, выводит «Управление» вперёд. Иначе оба
окна пишут в один `model.settings`, запись придержана для всего сразу, и
«Отменить» молча откатило бы микрофон, выбранный в соседнем окне.

| Что | Почему открыто |
| --- | --- |
| Микрофон, наушники, AGC, отпускание устройства | Меняется при смене наушников, то есть чаще всего остального |
| Список профилей + офис/удалённо + «Исправить сеть» | Менеджер уезжает домой сам; звонить ради этого в поддержку незачем |
| Рингтон: включение, громкость, устройство, свой файл, «Прослушать» | Звук на своём рабочем месте человек выбирает сам |
| Самопроверка голоса | Ответ на «меня слышно?» без звонка коллеге |
| «Собрать логи для поддержки» | Ради этого и делался M7a |

**Профиль выбирается, но не правится.** Завести, переименовать или удалить
профиль, сменить номер, домен, транспорт или SIP-пароль — административные
действия. Менеджеру нужно ровно одно: переключиться на тот профиль, который ему
выдали, и переехать с офиса на удалёнку.

**Список, а не выпадающий выбор, и три признака в строке: название, номер,
формат работы.** Порядок слева направо не случаен: сначала то, по чему выбирают,
потом то, по чему опознают. Название («Лаба», «Боевой») говорит, который профиль
«мой»; номер и офис/удалёнка говорят, куда он звонит, — по ним его ищут в
поддержке.

**Название стоит первым, обычным шрифтом, и правит его менеджер.** Именно по нему
профиль и выбирают: номер в списке из двух добавочных на одной АТС не различает
ничего. Набирать его мелким серым под номером — значит спрятать то, ради чего в
этот список смотрят.

**Нажатие в любое место строки переключает профиль.** Это главное действие
списка, и требовать попасть в галочку размером 15 pt значило бы сделать его
самым трудным.

Переименование поэтому спрятано за карандашом: строка, которая одновременно и
кнопка, и поле ввода, не годится ни на то, ни на другое, а править название
нужно куда реже, чем переключаться. Карандаш превращает название в поле и
меняется на галочку «готово».

`labelsHidden` у этого поля обязателен — в `Form` первый строковый аргумент
`TextField` становится подписью в левой колонке, и placeholder уезжает от своего
же поля через всю строку. Это уже было один раз и выглядело как два разных поля.

Иконки карандаша в комплекте не было: SF Symbols на Catalina нет вовсе, и все 26
значков нарисованы своими SVG. Двадцать седьмой — `pencil` — нарисован в том же
стиле: обводка 1.5, скруглённые стыки, сетка 20×20.

Отдельного «поля для заметки менеджера» рядом с меткой нет намеренно. Такое поле
заводилось и было убрано в тот же день: два поля с одинаковым смыслом расходятся,
и в поддержке потом непонятно, по какому из них человек опознаёт профиль. Метка
одна, и правят её оба — и администратор в «Управлении», и менеджер у себя.

Формат работы показывается разрешённым: у профиля с `.automatic` это ответ по
адресу сервера, а не слово «по адресу сервера» — вопрос должен быть закрыт, а не
переадресован обратно.

**Диагностика открыта только сбором архива.** Просмотр журнала, уровни записи,
срок хранения и путь к файлу настроек — закрыты. Номера лидов менеджер и так
видит в панели звонков, так что новой утечки сбор архива не создаёт, а разбор
жалобы без журнала — ровно та проблема, которую закрывал M7a.

### Что закрыто

Аккаунты и профили целиком, макросы DTMF, политика защиты от автокликеров, TLS
без проверки сертификата, вкладка диагностики и вкладка «Доступ».

## Пароль: одно значение

`AdminCredential` в пакете `AdminAccess` хранит **одно** значение — `loginHash`,
PBKDF2-HMAC-SHA256 от пароля со своей солью. По нему проверяется вход;
восстановить из него пароль нельзя. Файл настроек уезжает в бэкапы, и пароля в
нём быть не должно.

**PBKDF2, а не HKDF из CryptoKit.** `HKDF` появился в macOS 11, а срез x86_64
живёт с Catalina. И решает он другую задачу — растянуть уже случайный ключ, а не
удорожить перебор придуманного человеком пароля. Взят `CCKeyDerivationPBKDF` из
CommonCrypto, 150 000 итераций. Число хранится в самих учётных данных, чтобы его
можно было поднять, не сломав уже настроенные рабочие места.

## Второго значения больше нет: код восстановления убран

**Отменено 25 августа 2026.** До этого рядом с `loginHash` лежал `recoveryBox` —
тот же пароль, запечатанный ключом из шестизначного кода восстановления. Код был
зашит в сборку константой и равен 060397 у всех машин разом.

Долг был записан прямо здесь же, в разделе «Осталось честно сказать»: код
публичен для всякого, кто откроет бинарник через `strings`, то есть
«восстановление пароля» работало у любого, кто вскрыл `.app`. Ожидалось, что
код начнёт приезжать своим у каждой машины и станет настоящей защитой.

Случилось иначе, и лучше. **Административный пароль стал полем предустановки и
приезжает из панели** — помашинным объектом `access/<installation_id>`,
подписанным Ed25519. Забывший пароль администратор смотрит в панель, потому что
оттуда пароль и приезжает; расшифровывать что-то на самой машине больше не
нужно. Вместе с кодом ушли `RecoveryCode`, `RecoveryCodeSeal`,
`recoverySalt`/`recoveryBox`, вход по коду в `AdminUnlockView` и требование
ключа `recoveryCode` в `Tools/embed-provisioning.sh`.

Заодно это унесло файл конфигурации рабочего места: он запечатывался тем же
кодом, и был он ровно тем, от чего уходит вся линия ключей, — конторскими
настройками в локальном артефакте, применяемыми мимо панели. Разбор —
`elitesip-site/docs/DECISIONS.md`.

## В «Управлении» нет сохранения на горячую

Пока окно «Управление» открыто, на диск не уходит **ничего** — включая оба
пароля, SIP и административный.

Причина в том, что сохранение здесь означает не «записал громкость», а «объявил
машину настроенной вручную». Такое объявление должно быть отдельным действием, а
не побочным эффектом случайного клика по ползунку.

Отсюда весь порядок работы:

- **«Сохранить»** применяет всё разом, ставит `AdminManagement.local` и пишет
  строку в журнал. Перед этим спрашивает подтверждение — того же содержания, что
  и предупреждение на входе. Дублирование намеренное: между входом и нажатием
  проходит вся настройка рабочего места, и прочитанное на входе к этому моменту
  забыто.
- **«Отменить»** возвращает снимок, снятый при входе.
- **Крестик с несохранённым** спрашивает три варианта — сохранить, не сохранять,
  отмена. Молчаливый выброс правок отвергнут: цена случайного ⌘W — вся настройка
  чужого рабочего места. Запрет закрывать окно тоже отвергнут: он ломает привычку
  сильнее, чем помогает.

**Почему снимок, а не отдельная модель черновика.** Формы закрытых вкладок уже
написаны против `model.settings`. Переписать полторы тысячи строк на второй
источник значило бы завести два способа прочитать настройку — то есть однажды
прочитать не тот. Вместо этого придерживается запись на диск
(`isHoldingSettingsWrites`), а «Отменить» возвращает снимок.

**Честная цена этого решения.** Придерживается запись, а не изменение в памяти:
пока окно открыто, правки уже действуют на работающее приложение. Для громкости
и устройств это и нужно — иначе проверить их на слух было бы нельзя, — но смена
домена АТС в черновике повлияет на подключение до сохранения. «Отменить»
возвращает такие правки, а не предотвращает их.

Пароль SIP этой ценой не задет: он поле `settings`, и его запись придержана
вместе со всеми остальными — «Отменить» возвращает снимок вместе с ним. А вот
административный пароль в `settings` не лежит, поэтому у него своя отложенная
копия: `stageAdminPassword` кладёт его в `pending`-поле, и ящик с солью и хешем
меняется только на «Сохранить». Иначе «Отменить» отменяло бы не всё.

## Сессия живёт до закрытия окна

Согласовано. `AdminAccessState.isUnlocked` не сохраняется нигде: `restore`
всегда закрывает режим, `windowWillClose` окна настроек вызывает
`lockAdministration`.

Срок выбран именно таким: администратор настроил чужое рабочее место, закрыл
окно и ушёл — после этого закрытая часть снова закрыта, без таймеров и без
надежды на то, что он нажмёт «Выйти». Держать открытость в файле значило бы
однажды оставить рабочее место распахнутым после перезапуска, и узнать об этом
было бы неоткуда.

**Открыть режим можно только предъявив пароль.** `AdminAccessState` не даёт
создать себя открытым — `isUnlocked` нельзя задать через инициализатор, а внутри
структуры он закрыт на запись. Поэтому открытый сеттер `AppModel.adminAccess`
ничего не ослабляет: подделать состояние нечем.

## Без пароля всё открыто

Согласовано. Пока `credential == nil`, закрытая часть видна всем, и на вкладке
«Доступ» висит предупреждение об этом.

Причина продуктовая: свежее рабочее место настраивают целиком — аккаунт,
устройства, макросы, — и начинать эту настройку с придумывания пароля значит,
что первый же установщик пароль и не задаст. Пароль ставится последним
действием.

## Что пишется в журнал

Пункт 4 роадмапа. Отметку времени ставит `LogEntry`, отдельного форматирования
нет. В M9 те же строки уедут в панель EliteSIP.

| Событие | Уровень |
| --- | --- |
| `административный режим открыт` | info |
| `административный режим: неверный пароль` | warning |
| `административный режим закрыт` | info |
| `административный пароль задан` / `изменён` | info |
| `административный пароль снят, настройки открыты всем` | warning |
| `закрытые настройки сохранены` | info |
| `правки закрытых настроек отменены` | info |

**Неудачные попытки пишутся тоже.** Если рабочее место однажды окажется
открытым, разбирать будут по этим строкам, и «попыток не было» — такой же ответ,
как «их было двадцать».

**Самого пароля в журнале нет** — ни здесь, ни маскированием: строки с ним
просто не существует. Маскирование в `Diagnostics` вырезает известные имена
полей, и «пароль администратора» к ним не относится.

Выход из режима молчит, если режим и так был закрыт: закрытие окна настроек
происходит каждый раз, и строка «режим закрыт» после каждого взгляда на
громкость сделала бы журнал нечитаемым ровно в том месте, ради которого её
пишут.

## Локальный режим — явное состояние

Пункт 3 роадмапа. Был `AdminManagement` со значениями `local` и `configFile`;
хранился в настройках, показывался в подвале менеджерской страницы и на вкладке
«Доступ».

**EliteDash отсюда убран сознательно** (решение 3 августа 2026). Изначально
локальный режим противопоставлялся синхронизации с EliteDash, но EliteDash не
написан, а живой бета-тест нужен сейчас. Противопоставление заменили на файл
конфигурации — это был новый M8.

**`AdminManagement` убран целиком 25 августа 2026.** Файла конфигурации нет, и
`.configFile` не ставил больше никто; а с одним оставшимся значением тип начал
врать — машина, которой управляет панель, показывала бы «Локальный режим».

Управляемость машины живёт с тех пор там, где ей и место, — в
`AppSettings.PanelSettings`: режим `managed`/`manual`, предустановка, ревизия,
ключ канала. Показывают её два места: блок «Панель» над карточками профилей в
«Аккаунте» и строка «Кто управляет настройками» во вкладке «Доступ», которая
теперь читает ответ оттуда же. Пакет `AdminAccess` о панели не знает и знать не
должен: он про пароль.

## Самопроверка голоса

Пять секунд записи, сразу воспроизведение. `VoiceSelfTest` в `MediaCore`.

**Через тот же `VoiceAudioEngine`, что и разговор.** Отдельный `AVAudioEngine`
был бы проще, но проверял бы не то, что ломается: ломается связка «выбранный
микрофон + выбранные наушники + обработка голоса + приватное агрегатное
устройство», а не умение macOS писать звук. Записанные кадры — те же кодированные
пакеты, что ушли бы в RTP; воспроизводятся они через тот же `onNeedsFrame`, что
и приехавшие от собеседника.

Запись живёт в памяти и стирается сразу после воспроизведения. На диск не
попадает ничего: в пяти секундах вполне может оказаться чужой голос.

Обратный отсчёт считается по числу записанных кадров, а не по таймеру. Если
звуковая карта встала, отсчёт встаёт вместе с ней, а не дорисовывает нули на
экране.

Проверка не запускается в разговоре: устройство занято трактом. Живой входящий
её отменяет — микрофон и наушники нужнее ему.

## Замена рингтона

`RingtoneSettings.customSoundPath`. Обычный путь, а не закладка безопасности:
App Sandbox выключен осознанно (см. `EliteSIP.entitlements`), и закладка была бы
обвязкой без причины.

Файл читается целиком в буфер и зацикливается одним планированием — никакое
чтение с диска не встаёт между «пришёл вызов» и «зазвонило». Длинный файл этим
ограничен, и ограничение по делу: рингтон на минуту в памяти держать незачем.

**Пропавший файл не ломает звонок.** Рингтон молча возвращается к
синтезированному: беззвучный входящий хуже неожиданного. В настройках при этом
написано «файл не найден» — человек должен понимать, почему звук не тот.

`NSOpenPanel`, а не `fileImporter`: тот появился в macOS 11.

Предпрослушивание идёт через тот же `Ringtone`, и живой входящий его глушит:
`Ringtone.start` на занятом движке молча ничего не делает, то есть настоящий
вызов пришёл бы беззвучно, если бы предпрослушивание осталось играть.

## Схема настроек не выросла

Поле `admin` добавлено, `schemaVersion` остался равным 2. Старый файл читается
терпимым декодером и получает `AdminSettings()` — «пароль не задан, управляет
администратор этой машины», то есть ровно прежнее поведение. Версия схемы растёт
от несовместимости, а не от прибавления.

Испорченный блок доступа читается как «пароля нет» и не роняет весь файл: иначе
одна битая строка стоила бы учётной записи. Открытые настройки на машине, где
пароль был, заметят сразу — в отличие от потерянного профиля.

## Две дыры, найденные на живой машине 17 августа 2026

Обе про одно: приложение **не спрашивало административный пароль**, хотя он был
задан. Нашёл их заказчик, а не тесты и не аудит, — и обе стоят того, чтобы быть
записанными, потому что каждая держалась на своей мелочи.

**«Управление», закрытое кнопкой, оставляло режим открытым.** Гасил режим
`windowWillClose`, но по этому пути закрытие до делегата не доходит:
`closeAdministrationWindow` снимает делегата окна **до** `close()` — нужно, чтобы
закрытие по «Сохранить» и «Отменить» не приняли за крестик с его вопросом о
несохранённом. То есть после первого входа пароль не спрашивался до конца смены:
администратор уходил, а закрытая часть оставалась открытой — ровно то, от чего
режим и заведён. Теперь режим гасится в самом `closeAdministrationWindow`.

**После мастера первоначальной настройки пароля не существовало для живого
процесса.** Мастер записывал учётные данные в настройки, но `AdminAccess` читает
их только при запуске (`AppModel.init`), и до перезапуска приложение считало
машину незащищённой — а `AdminUnlockView` на незащищённой машине входит сам, без
вопроса. Мастер при этом заканчивается **без** перезапуска, если стекло не
меняли, то есть дыра жила до конца смены. Теперь мастер вызывает
`adminAccess.restore(credential:management:)` там же, где пишет настройки.

Общий вывод, который стоит помнить: **у административного режима два состояния —
записанное в настройках и живущее в процессе, — и правка одного без другого
выглядит как работа.** Оба случая именно так и выглядели.

## Осталось честно сказать

- ~~**Код восстановления один на все машины и лежит в бинарнике.**~~ Закрыто
  25 августа 2026 — не тем способом, который тут предполагался: код убран
  совсем, а административный пароль стал полем предустановки и приезжает из
  панели. См. «Второго значения больше нет».
- **Черновик придерживает запись, а не изменение.** Подробности выше; смена домена
  АТС в черновике повлияет на подключение до сохранения.
- **Прогона на стенде у режима нет.** Проверено сборкой, 14 тестами пакета
  `AdminAccess` и `Tools/check-compat.sh` на обеих архитектурах. Живой проверки
  «задать пароль, закрыть окно, открыть заново, войти» на настоящем рабочем
  месте ещё не было.
- **Смена пароля на машине разводит её с панелью.** Панель считает действующим
  тот пароль, который лежит в предустановке, и при следующей выкладке доступа
  вернёт его. Менять пароль надо в панели, а не здесь.
- **Самопроверка голоса не измеряет задержку.** Она отвечает на «слышно?», а не
  на «насколько с опозданием». Замеры задержки — задача M7f на живом Intel.
