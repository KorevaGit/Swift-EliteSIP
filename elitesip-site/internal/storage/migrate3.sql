-- Третья версия схемы: роли пользователей панели.
--
-- Свежая база получает это прямо из schema.sql. Здесь — только перевод той,
-- что уже живёт на сервере конторы.

-- Все, кто уже заведён, остаются администраторами: понизить их молча значило
-- бы в момент обновления отобрать у работающих людей права, которых у них до
-- сих пор никто не отбирал.
ALTER TABLE admins ADD COLUMN role TEXT NOT NULL DEFAULT 'admin';
